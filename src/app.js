import React from 'react'
import ReactDOM from 'react-dom'
import Header from './components/Header'

let rootElement = document.getElementById('root')

ReactDOM.render(<Header title="ReactJs is awesome!"></Header>, rootElement);